node_modules
.next
.turbo
dist
.env
.env.*
!.env.example
*.local
coverage
*.tsbuildinfo
ogun-roadmap.md
\.claude
faz-9-masaustu-kabugu.md
faz-10-ui-cilasi.md

# GitHub issue #45 / Prompt 8.1 — Playwright E2E
apps/e2e/playwright-report
apps/e2e/test-results
apps/e2e/fixtures/.e2e-credentials.json
apps/web/.next/analyze
apps/web/.lighthouse-reports

# GitHub issue #51 / Prompt 9.1 — Tauri masaüstü kabuğu (apps/desktop).
# Rust derleme çıktısı, prepare-sidecar.mjs'in ürettiği (apps/web'in
# standalone build'i + yerel Node ikili dosyası kopyası içeren) yerel
# kaynaklar ve Tauri'nin otomatik ürettiği izin şemaları — hiçbiri
# kaynak kontrolüne GİRMEMELİ (bkz. apps/desktop/scripts/*.mjs).
apps/desktop/src-tauri/target
apps/desktop/src-tauri/gen
apps/desktop/src-tauri/resources
apps/desktop/src-tauri/binaries

# GitHub issue #54 / Prompt 9.4 — .github/workflows/desktop-release.yml'in
# release build sırasında ÜRETTİĞİ geçici updater imzalama yapılandırması
# (bkz. o workflow'un "Güncelleme yapılandırmasını hazırla" adımı) — genel
# anahtar İÇEREBİLİR (özel anahtar DEĞİL, ama YİNE DE kaynak kontrolüne
# GİRMEMELİ, sadece CI'da üretilip tüketilen bir ARA dosya).
apps/desktop/release-updater.conf.json
NASIL-CALISTIRIRIM.md
