{
  "name": "desktop",
  "version": "0.1.9",
  "private": true,
  "description": "Öğün masaüstü kabuğu — apps/web'i saran Tauri 2.x native pencere (GitHub issue #51).",
  "scripts": {
    "tauri": "tauri",
    "dev": "wait-on tcp:3000 && tauri dev",
    "icons": "node scripts/generate-icons.mjs",
    "build": "tauri build",
    "signer:generate": "tauri signer generate -w ~/.tauri/ogun-updater.key",
    "typecheck": "echo 'apps/desktop: Rust tarafı `cargo check` ile doğrulanır, JS tarafında derlenecek TypeScript yok' && exit 0",
    "lint": "echo 'apps/desktop: JS tarafında lint edilecek uygulama kodu yok (yalnızca build script\\'leri)' && exit 0",
    "test": "echo 'apps/desktop: bu paket için otomatik test yok (bkz. README.md doğrulama notları)' && exit 0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "wait-on": "^8.0.5"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  }
}
